@model MiniCar_Model.Models.ViewModels.CartHeaderVM

@if (Model.TotalQuantity > 0)
{
  <em class="cart-badge badge bg-danger rounded-pill position-absolute"
      style="top: 50%; right: -13px; transform: translateY(30%);">
    @Model.TotalQuantity
  </em>
}
